<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@id/refresh"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@id/playlist"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent" />
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <include
        android:id="@id/fast_scroller"
        layout="@layout/fast_scroller" />

    <include
        android:id="@id/content_progress"
        layout="@layout/progress"
        android:visibility="gone" />

    <LinearLayout
        android:id="@id/playingq_empty"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@id/playingq_icon_no_data"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/app_name"
            android:src="@drawable/bgicon_nodata" />

        <TextView
            android:id="@id/playingq_tv_no_data"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="25.0dip"
            android:layout_marginRight="25.0dip"
            android:gravity="center"
            android:singleLine="false"
            android:text="@string/msg_playing_queue_no_music"
            android:textColor="@color/text_desc"
            android:textSize="@dimen/no_data_desc" />
    </LinearLayout>
</FrameLayout>